<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="CircleImageView">
        <attr name="border_width_circle" format="dimension" />
        <attr name="border_color_circle" format="color" />
    </declare-styleable>

    <!-- used to set the minTextSize via xml -->
    <!-- <attr format="reference" name="autoScaleTextViewStyle"/> -->
    <!-- <declare-styleable name="AutoScaleTextView"> -->
    <!-- <attr format="dimension" name="minTextSize"/> -->
    <!-- </declare-styleable> -->

    <declare-styleable name="AutofitTextView">

        <!-- Minimum size of the text. -->
        <attr name="minTextSize" format="dimension" />
        <!-- Amount of precision used to calculate the correct text size to fit within its bounds. Lower precision is more precise and takes more time. -->
        <attr name="precision" format="float" />
        <!-- Defines whether to automatically resize text to fit to the view's bounds. -->
        <attr name="sizeToFit" format="boolean" />
    </declare-styleable>

</resources>