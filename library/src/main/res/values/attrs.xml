<?xml version="1.0" encoding="utf-8"?>
<resources> 
    <declare-styleable name="match">
        <attr name="text" format="string" />
        <attr name="textColor" format="color" />
        <attr name="textSize" format="dimension" />
    </declare-styleable>
    
    <declare-styleable name="ExtendViewPager">
        <attr name="style">
            <enum name="standard" value="0" />
            <enum name="tablet" value="1" />
            <enum name="cubein" value="2" />
            <enum name="cubeout" value="3" />
            <enum name="flipvertical" value="4" />
            <enum name="fliphorizontal" value="5" />
            <enum name="stack" value="6" />
            <enum name="zoomin" value="7" />
            <enum name="zoomout" value="8" />
            <enum name="rotateup" value="9" />
            <enum name="rotatedown" value="10" />
            <enum name="accordion" value="11" />
        </attr>
        <attr name="fadeEnabled" format="boolean" />
        <attr name="outlineEnabled" format="boolean" />
        <attr name="outlineColor" format="color|reference" />
    </declare-styleable>
    
     <declare-styleable name="TabFootView">
        <attr name="title_background" format="reference"></attr>
        <attr name="tab_count" format="integer"></attr>
        <attr name="tab_backgroud" format="reference"></attr>
        <attr name="tab_divider" format="integer"></attr>
        <attr name="tab_anim_duration" format="integer"></attr>
        <attr name="tab_title_foot_divider" format="integer"></attr>
        <attr name="tabClickEnable" format="boolean"></attr>
    </declare-styleable>
    
     <declare-styleable name="FrameAnimationView">
         <attr name="path" format="string"></attr>
         <attr name="extend_name" format="string"></attr>
         <attr name="frame_count" format="integer"></attr>
         <attr name="frame_duration" format="integer"></attr>
         <attr name="speed_hand" format="integer"></attr>
         <attr name="agent_search" format="boolean"></attr>
         <attr name="pic_name_length" format="integer"></attr>
     </declare-styleable>
     
   <declare-styleable name="ImageAnimationView">
     <attr name="animType_visible">
           <enum name="zoom_in" value="0" />
           <enum name="zoom_out" value="1" />
           <enum name="left_to_right" value="2" />
           <enum name="right_to_left" value="3" />
           <enum name="up_to_down" value="4" />
           <enum name="down_to_up" value="5" />
     </attr>       
     <attr name="animType_gone">
           <enum name="zoom_in" value="0" />
           <enum name="zoom_out" value="1" />
           <enum name="left_to_right" value="2" />
           <enum name="right_to_left" value="3" />
           <enum name="up_to_down" value="4" />
           <enum name="down_to_up" value="5" />
     </attr>          
     <attr name="duration_visible" format="integer"></attr>
     <attr name="duration_gone" format="integer"></attr>
     <attr name="interpolator_visible" format="reference"></attr>
     <attr name="interpolator_gone" format="reference"></attr>
   </declare-styleable>
 
    <!-- Defines the custom XML attributes supported for a HorizontalListView -->
    <declare-styleable name="HorizontalListView">
        <attr name="dividerWidth" format="dimension" />
        <attr name="android:divider" />
    </declare-styleable>
    
    <declare-styleable name="CustomFastScrollView">
        <attr name="overlayWidth" format="dimension" />
        <attr name="overlayHeight" format="dimension" />
        <attr name="overlayTextSize" format="dimension" />
        <attr name="overlayScrollThumbWidth" format="dimension" />
    </declare-styleable>
    
    
    <!-- Reference to a style for the menu drawer. -->
    <attr name="menuDrawerStyle" format="reference" />

    <!-- Styleables used for styling the menu drawer. -->
    <declare-styleable name="MenuDrawer">

        <attr name="mdContentBackground" format="reference" />

        <!-- Drawable to use for the background of the menu. -->
        <attr name="mdMenuBackground" format="reference" />

        <!-- The size of the menu. -->
        <attr name="mdMenuSize" format="dimension" />

        <!-- Drawable used as indicator for the active view. -->
        <attr name="mdActiveIndicator" format="reference" />

        <!-- Defines whether the content will have a dropshadow onto the menu. Default is true. -->
        <attr name="mdDropShadowEnabled" format="boolean" />

        <!-- The size of the drop shadow. Default is 6dp -->
        <attr name="mdDropShadowSize" format="dimension" />

        <!-- The color of the drop shadow. Default is #FF000000. -->
        <attr name="mdDropShadowColor" format="color" />

        <!-- Drawable used for the drop shadow. -->
        <attr name="mdDropShadow" format="reference" />

        <!-- The touch bezel size. -->
        <attr name="mdTouchBezelSize" format="dimension" />

        <!-- Whether the indicator should be animated between active views. -->
        <attr name="mdAllowIndicatorAnimation" format="boolean" />

        <!-- The maximum animation duration -->
        <attr name="mdMaxAnimationDuration" format="integer" />

        <!-- Drawable that replaces the up indicator -->
        <attr name="mdSlideDrawable" format="reference" />

        <!-- String to use as the up indicators content description when the drawer is open -->
        <attr name="mdDrawerOpenUpContentDescription" format="string" />

        <!-- String to use as the up indicators content description when the drawer is closed -->
        <attr name="mdDrawerClosedUpContentDescription" format="string" />

        <!-- Whether an overlay should be drawn as the drawer is opened and closed -->
        <attr name="mdDrawOverlay" format="boolean" />

        <!-- The position of the drawer -->
        <attr name="mdPosition" format="enum">
            <enum name="left" value="0" />
            <enum name="top" value="1" />
            <enum name="right" value="2" />
            <enum name="bottom" value="3" />
            <enum name="start" value="4" />
            <enum name="end" value="5" />
        </attr>
    </declare-styleable>
    
    
    <declare-styleable name="Circle">
        <attr name="circleBackground" format="integer" />
        <attr name="firstChildPosition">
            <enum name="East" value="0" />
            <enum name="South" value="90" />
            <enum name="West" value="180" />
            <enum name="North" value="270" />
        </attr>
        <attr name="rotateToCenter" format="boolean" />
        <attr name="isRotating" format="boolean" />
    </declare-styleable>
    <declare-styleable name="CircleImageView">
        <attr name="name" format="string" />
    </declare-styleable>
    
    
    
    <declare-styleable name="GlowPadView">
        <attr name="android:gravity"/>

        <!-- Reference to an array resource that be shown as targets around a circle. -->
        <attr name="targetDrawables" format="reference"/>

        <!-- Reference to an array resource that be used as description for the targets around the circle. -->
        <attr name="targetDescriptions" format="reference"/>

        <!-- Reference to an array resource that be used to announce the directions with targets around the circle. -->
        <attr name="directionDescriptions" format="reference"/>

        <!-- Sets a drawable as the center. -->
        <attr name="handleDrawable" format="reference"/>

        <!-- Drawable to use for wave ripple animation. -->
        <attr name="outerRingDrawable" format="reference"/>

        <!-- Drawble used for drawing points -->
        <attr name="pointDrawable" format="reference"/>

        <!-- Inner radius of glow area. -->
        <attr name="innerRadius" format="dimension"/>

        <!-- Outer radius of glow area. Target icons will be drawn on this circle. -->
        <attr name="outerRadius" format="dimension"/>

        <!-- Size of target radius. Points within this distance of target center is a "hit". -->
        <!--
        <attr name="hitRadius" format="dimension"/>
        -->

        <!-- Radius of glow under finger. -->
        <attr name="glowRadius" format="dimension"/>

        <!-- Tactile feedback duration for actions. Set to '0' for no vibration. -->
        <attr name="vibrationDuration" format="integer"/>

        <!-- How close we need to be before snapping to a target. -->
        <attr name="snapMargin" format="dimension"/>

        <!-- Number of waves/chevrons to show in animation. -->
        <attr name="feedbackCount" format="integer"/>

        <!-- Used when the handle shouldn't wait to be hit before following the finger -->
        <attr name="alwaysTrackFinger" format="boolean"/>
    </declare-styleable>

    <declare-styleable name="AnalogClock">
        <attr name="jewelRadius" format="dimension"/>
        <attr name="jewelOffset" format="dimension"/>
        <attr name="jewelColor" format="color"/>
    </declare-styleable>
    
    <declare-styleable name="TabBarView">
        <attr name="tabViewId" format="reference"/>
        <attr name="repeatClickEnable" format="boolean"/>
        <attr name="defaultSelectIndex" format="integer"/>
    </declare-styleable>
</resources>
